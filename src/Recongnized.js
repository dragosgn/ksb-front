import React from "react";
import styled from "styled-components";

const Root = styled.div`
  width: 100%;
  height: 100%;
`;

export default ({ currentView, setCurrentView }) => <Root>sdsadas</Root>;
